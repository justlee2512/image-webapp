{
  "name": "image-drive",
  "version": "2.0.0",
  "private": true,
  "description": "Secure and responsive image storage app backed by PostgreSQL",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/security.js && node --check public/app.js"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.5.0",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "express-session": "^1.18.1",
    "helmet": "^8.1.0",
    "multer": "^2.0.1",
    "pg": "^8.16.0",
    "sharp": "^0.34.3"
  }
}
